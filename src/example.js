let addition = function additionFunction(a,b) {
  return a + b;
}

let subtraction = function subtractionFunction(a,b) {
  return a - b;
}

let multiply = function multiplyFunction(a,b) {
  return a * b;
}

module.exports = {
  addition,
  subtraction,
  multiply
}
